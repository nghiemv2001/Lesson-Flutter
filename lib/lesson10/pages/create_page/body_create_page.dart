import 'package:flutter/material.dart';

class BodyCreatePage extends StatelessWidget {
  const BodyCreatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(
        child: Text("Create page"),
      );
}
